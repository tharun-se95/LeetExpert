# Prompt: Improve Explanations

Copy this into chat, point it at a chapter, and send it.

---

Re-read `<FILE PATH>` purely for teaching quality (not formatting).

For each section, check whether a reader with basic programming knowledge but
no memory of this specific pattern would actually understand:

- **Why** the naive approach fails (concretely, not just "it's slow")
- **What** insight makes the better approach possible
- **When** to reach for this pattern from problem wording alone
- **Where** it shows up outside of interviews

Rewrite any section that relies on the reader already knowing the answer.
Replace weak or generic analogies with sharper, more concrete ones. Tighten
any paragraph that could be cut without losing meaning.

Do not change the section structure or add/remove sections — only improve the
explanations within the existing structure.
